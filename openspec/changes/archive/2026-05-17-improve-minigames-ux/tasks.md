## 1. RhymeSquad Enhancements

- [x] 1.1 Modify the `.bubble-turbo` CSS class in `RhymeSquad.vue` to remove the `@keyframes jitter` shaking animation and yellow background, replacing it with a stable, highly readable, glowing high-tech frame.
- [x] 1.2 Increase the vertical spacing and falling margins between consecutive words in `RhymeSquad.vue` to maximize user reaction time.

## 2. RadioSignal Interactive Controls

- [x] 2.1 Integrate vintage radio dashboard control widgets (Volume Dial knob, AM/FM Band switch, and Signal Filter switch) into the `RadioSignal.vue` template.
- [x] 2.2 Declare the corresponding local Vue state reactive variables (`radioVolume`, `isFilterActive`, `bandSelected`) and hook them to toggle retro dashboard indicators.

## 3. RadarScan Glowing Boundaries

- [x] 3.1 Modify the `.board` container CSS rules in `RadarScan.vue` to add a tactical, glowing neon-blue/cyan perimeter dashed border defining the active play grid boundaries.
