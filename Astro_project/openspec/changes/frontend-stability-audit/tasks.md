## 1. Auditoría de Seguridad de Sala (Room Access)

- [x] 1.1 Aplicar `?.` en `RadarScan.vue` para accesos a `multiplayerStore.room`.
- [x] 1.2 Aplicar `?.` en `RadioSignal.vue` para accesos a `multiplayerStore.room`.
- [x] 1.3 Aplicar `?.` en `SpelledRosco.vue` para accesos a `multiplayerStore.room`.
- [x] 1.4 Aplicar `?.` en `WordConstruction.vue` para accesos a `multiplayerStore.room`.
- [x] 1.5 Aplicar `?.` en `MatchResultScreen.vue` para accesos a `multiplayerStore.room`.

## 2. Refuerzo de RhymeSquad y Lobby

- [x] 2.1 Verificar importaciones de `ref`, `computed` y `watch` en `RhymeSquad.vue`.
- [x] 2.2 Confirmar que no existen advertencias de consola en `MultiplayerLobby.vue` tras los cambios de variante.

## 3. Verificación Global

- [x] 3.1 Ejecutar `npm run lint` en el frontend para detectar importaciones faltantes o variables no utilizadas.
- [x] 3.2 Realizar una prueba visual de los bordes punteados en el lobby.
